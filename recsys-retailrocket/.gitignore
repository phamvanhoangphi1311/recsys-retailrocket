# Data — quá lớn, lưu S3
data/
*.parquet
*.npy
*.csv

# Model artifacts — lưu MLflow/S3
*.pt              ← PyTorch model weights
*.bin             ← Gensim model
*.txt             ← LightGBM model (lgbm_ranker.txt)
*.index           ← FAISS index files

# Python
__pycache__/
*.pyc
.env              ← secrets (AWS keys, API keys) — TUYỆT ĐỐI KHÔNG push
venv/

# Docker
*.tar             ← exported docker images

# IDE
.idea/
.vscode/
*.ipynb_checkpoints/